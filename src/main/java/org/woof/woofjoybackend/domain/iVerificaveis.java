package org.woof.woofjoybackend.domain;

public interface iVerificaveis {

//    default int transformaIdEmIndex(int id, List<Usuario> list) {
//        for (int i = 0; i < list.size(); i++) {
//            if (id == list.get(i).getId()) {
//                return i;
//            }
//        }
//        return -1;
//    }
//    default int transformaIdEmIndexPet(int id, List<Pet> list) {
//        for (int i = 0; i < list.size(); i++) {
//            if (id == list.get(i).getId()) {
//                return i;
//            }
//        }
//        return -1;
//    }
//    default int transformaIdEmIndexItem(int id, List<Item> list) {
//        for (int i = 0; i < list.size(); i++) {
//            if (id == list.get(i).getId()) {
//                return i;
//            }
//        }
//        return -1;
//    }

//    default boolean verificarEmailSenha(Usuario usuario) {
//        String email = usuario.getEmail();
//        String senha = usuario.getSenha();
//        return ( email != null
//                && !email.isBlank()
//                && email.contains("@")
//                && !senha.isBlank()
//                && senha != null
//                && senha.length() > 4);
//    }
//
//    default boolean verificarEmailSenha(String email, String senha) {
//        return (!email.isBlank()
//                && email != null
//                && email.contains("@")
//                && !senha.isBlank()
//                && senha != null
//                && senha.length() > 4);
//    }
}
