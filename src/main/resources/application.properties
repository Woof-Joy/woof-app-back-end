# Exibe mensagens de erro valores invalidos (400)
server.error.include-message=always
server.error.include-binding-errors=always

# MYSQL
# SOBE TODA TABELA MAPEADA AO RODAR APLICACAO
spring.jpa.hibernate.ddl-auto=update
# Configurações para o banco de dados RDS
spring.datasource.rds.url=jdbc:mysql://rds-woofjoy.cv6u0io08hrw.us-east-1.rds.amazonaws.com:3306/woofjoy
spring.datasource.rds.username=admin
spring.datasource.rds.password=gnb12345

# Configurações para o banco de dados local
spring.datasource.local.url=jdbc:mysql://localhost:3307/wf_db
spring.datasource.local.username=root
spring.datasource.local.password=bp1234

# Configuração para determinar qual banco de dados usar
spring.profiles.active=local

# Configuração de condicional para selecionar o banco de dados com base no perfil ativo
spring.datasource.url=${spring.datasource.${spring.profiles.active}.url}
spring.datasource.username=${spring.datasource.${spring.profiles.active}.username}
spring.datasource.password=${spring.datasource.${spring.profiles.active}.password}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# habilita a impressao dos comando SQL no log
spring.jpa.show-sql=true


# Habilita data.sql
spring.jpa.defer-datasource-initialization=true

# formata os SQL (quebra linha, identa etc)
spring.jpa.properties.hibernate.format_sql=true

#validate do token
jwt.validity=3600000

# secret do token (minimo 32 caracteres)
jwt.secret=Q8rtqqHYaIRT9bvAb0OhFq6bHO36gEm7HuFBKscI756c2mIc6XeLwTPPmGQ2CJO


spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB